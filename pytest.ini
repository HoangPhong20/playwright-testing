[pytest]
addopts = -ra -q --html=reports/report.html --self-contained-html
testpaths = tests
markers =
    smoke: Quick smoke coverage for critical flows.
    regression: Broader regression coverage.
    cart: Cart-specific scenarios.
